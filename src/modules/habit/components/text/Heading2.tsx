import styled from "styled-components"
import { TypographyBase } from "./TypographyBase"

const Heading2 = styled(TypographyBase).attrs((props) => ({ variant: "h2", ...props }))``

export default Heading2
